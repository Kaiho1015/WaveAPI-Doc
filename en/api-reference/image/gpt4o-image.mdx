---
title: "GPT-4o Image Generation"
description: "Generate high-quality images using the GPT-4o-image model, supporting text-to-image, image-to-image, and image editing"
api: "POST https://ai.kaiho.cc/v1/tasks"
---

<Note>
  **Async processing mode:** A task ID is returned after submission. Use the [Query Task Status](/en/api-reference/task/get-status) endpoint to retrieve the generation result. Generated image links are valid for 24 hours -- please save them promptly.
</Note>

<RequestExample>
  ```bash cURL theme={null}
  curl --request POST \
    --url https://ai.kaiho.cc/v1/tasks \
    --header 'Authorization: Bearer <token>' \
    --header 'Content-Type: application/json' \
    --data '{
      "model": "gpt-4o-image",
      "prompt": "星空下的古老城堡，数字艺术风格",
      "size": "1:1",
      "n": 1,
      "image_urls": [
        "https://example.com/reference.png"
      ]
    }'
  ```

  ```python Python theme={null}
  import requests

  url = "https://ai.kaiho.cc/v1/tasks"

  payload = {
      "model": "gpt-4o-image",
      "prompt": "星空下的古老城堡，数字艺术风格",
      "size": "1:1",
      "n": 1,
      "image_urls": [
          "https://example.com/reference.png"
      ]
  }

  headers = {
      "Authorization": "Bearer <token>",
      "Content-Type": "application/json"
  }

  response = requests.post(url, json=payload, headers=headers)

  print(response.json())
  ```

  ```javascript JavaScript theme={null}
  const url = "https://ai.kaiho.cc/v1/tasks";

  const payload = {
    model: "gpt-4o-image",
    prompt: "星空下的古老城堡，数字艺术风格",
    size: "1:1",
    n: 1,
    image_urls: [
      "https://example.com/reference.png"
    ]
  };

  const headers = {
    "Authorization": "Bearer <token>",
    "Content-Type": "application/json"
  };

  fetch(url, {
    method: "POST",
    headers: headers,
    body: JSON.stringify(payload)
  })
    .then(response => response.json())
    .then(data => console.log(data))
    .catch(error => console.error('Error:', error));
  ```

  ```go Go theme={null}
  package main

  import (
      "bytes"
      "encoding/json"
      "fmt"
      "io/ioutil"
      "net/http"
  )

  func main() {
      url := "https://ai.kaiho.cc/v1/tasks"

      payload := map[string]interface{}{
          "model":  "gpt-4o-image",
          "prompt": "星空下的古老城堡，数字艺术风格",
          "size":   "1:1",
          "n":      1,
          "image_urls": []string{
              "https://example.com/reference.png",
          },
      }

      jsonData, _ := json.Marshal(payload)

      req, _ := http.NewRequest("POST", url, bytes.NewBuffer(jsonData))
      req.Header.Set("Authorization", "Bearer <token>")
      req.Header.Set("Content-Type", "application/json")

      client := &http.Client{}
      resp, err := client.Do(req)
      if err != nil {
          panic(err)
      }
      defer resp.Body.Close()

      body, _ := ioutil.ReadAll(resp.Body)
      fmt.Println(string(body))
  }
  ```
</RequestExample>

<ResponseExample>
  ```json 200 theme={null}
  {
    "code": "success",
    "data": "task-wave17486161910001884721"
  }
  ```

  ```json 400 theme={null}
  {
    "code": "invalid_request",
    "message": "Invalid request parameters",
    "data": null
  }
  ```

  ```json 401 theme={null}
  {
    "code": "authentication_error",
    "message": "Authentication failed. Please check your API key",
    "data": null
  }
  ```

  ```json 402 theme={null}
  {
    "code": "payment_required",
    "message": "Insufficient account balance. Please top up and try again",
    "data": null
  }
  ```

  ```json 403 theme={null}
  {
    "code": "quota_not_enough",
    "message": "Insufficient user quota",
    "data": null
  }
  ```

  ```json 429 theme={null}
  {
    "code": "rate_limit_error",
    "message": "Too many requests. Please try again later",
    "data": null
  }
  ```

  ```json 500 theme={null}
  {
    "code": "server_error",
    "message": "Internal server error. Please retry later",
    "data": null
  }
  ```
</ResponseExample>

## Authentication

<ParamField header="Authorization" type="string" required>
  All endpoints require Bearer Token authentication

  Obtain an API Key:

  Visit the [API Key management page](https://ai.kaiho.cc/dashboard/keys) to get your API Key

  Add the following to your request headers:

  ```
  Authorization: Bearer YOUR_API_KEY
  ```
</ParamField>

## Request Parameters

<ParamField body="model" type="string" default="gpt-4o-image" required>
  Image generation model name

  Example: `"gpt-4o-image"`
</ParamField>

<ParamField body="prompt" type="string" required>
  Text description for image generation

  Maximum 1000 characters
</ParamField>

<ParamField body="size" type="string">
  Image generation size

  Supported ratios:

  * `1:1` - Square
  * `2:3` - Portrait
  * `3:2` - Landscape
</ParamField>

<ParamField body="n" type="integer">
  Number of images to generate

  Supports 1, 2, or 4. Costs are pre-deducted based on quantity.

  Default: 1

  **Warning:** Must be a plain number (e.g., `1`), do not wrap in quotes
</ParamField>

<ParamField body="image_urls" type="array<url>">
  Reference image URL list for image-to-image generation or image editing

  <Expandable title="Details">
    Each element in the array is a string, supporting two formats:

    **1. Full image URL**

    * Publicly accessible image URL (http:// or https://)
    * Example: `"https://example.com/image.jpg"`

    **2. Base64-encoded format**

    * **Must use the complete Data URI format**
    * Format: `data:image/{format};base64,{base64data}`
    * Supported image formats: jpeg, png, webp
    * Example: `"data:image/jpeg;base64,/9j/4AAQSkZJRgABAQEAYABg..."`
    * Warning: Must include the `data:image/jpeg;base64,` prefix

    **Limits:**

    * Maximum 10MB per image
    * Supported formats: .jpeg, .jpg, .png, .webp
  </Expandable>

  **Limit:** Maximum 5 images
</ParamField>

<ParamField body="mask_url" type="string">
  Mask image URL

  * Must be PNG format
  * Dimensions must match the reference image
  * Maximum 4MB
</ParamField>

## Response Parameters

<ResponseField name="code" type="string">
  Response status code

  * `success` - Request successful, task submitted
</ResponseField>

<ResponseField name="data" type="string">
  Task ID, formatted as `task-wave{timestamp}{userId}{random}`

  Used to retrieve generation results via the [Query Task Status](/en/api-reference/task/get-status) endpoint
</ResponseField>

## Important Notes

1. **Async processing:** Tasks are processed asynchronously after submission. Use [Query Task Status](/en/api-reference/task/get-status) to retrieve results
2. **Link expiration:** Generated image links are valid for 24 hours. Please download and save them promptly
3. **Image URL requirements:** Reference images must be publicly accessible URLs or in Base64 format
4. **Billing:** When `n` is greater than 1, costs are pre-deducted based on quantity
