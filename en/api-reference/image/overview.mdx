---
title: "Image Generation Overview"
description: "Generate images through the unified /v1/tasks endpoint with support for multiple models"
---

## Overview

WaveAPI supports multiple image generation models, all accessible through the unified async task endpoint `/v1/tasks`.

## Request Endpoint

```
POST https://www.qingbo.dev/v1/tasks
```

## Common Request Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `model` | string | Yes | Model name |
| `action` | string | No | Operation type, defaults to `"generate"` |
| `prompt` | string | Yes | Image description text |
| `negative_prompt` | string | No | Content you do not want to appear |
| `n` | integer | No | Number of images to generate, default 1 |
| `size` | string | No | Aspect ratio, e.g., `"16:9"` |
| `resolution` | string | No | Resolution, e.g., `"2K"` |
| `image_urls` | array | No | Reference image URL array |
| `callback_url` | string | No | Webhook callback URL upon completion |
| `extra` | object | No | Model-specific pass-through parameters |

## Response Format

After task submission, a `task_id` is returned. Query results via `GET /v1/tasks/{task_id}`:

```json
{
  "task_id": "task-wave1775285160b950328499",
  "model": "doubao-seedream-5.0",
  "action": "generate",
  "status": "queued",
  "created_at": 1775285160040,
  "progress": 0
}
```

## Supported Models

<CardGroup cols={2}>
  <Card title="Seedream Series" href="/en/api-reference/image/seedream">
    ByteDance Seedream 5.0 / 4.5 / 4.0
  </Card>
  <Card title="Gemini Image Series" href="/en/api-reference/image/gemini">
    Gemini 2.5 Flash / 3 Pro / 3.1 Flash
  </Card>
  <Card title="GPT-4o Image" href="/en/api-reference/image/gpt4o-image">
    OpenAI image generation (coming soon)
  </Card>
  <Card title="FLUX Series" href="/en/api-reference/image/flux">
    FLUX Kontext / FLUX 2.0 (coming soon)
  </Card>
</CardGroup>

<Tip>
Each model supports different parameters. The `extra` field is used to pass model-specific parameters. Refer to each model's documentation page for detailed parameter support.
</Tip>
