---
title: "Seedream 5.0 Lite 图像生成"
description: "使用字节跳动 Seedream 5.0-Lite 模型生成高质量图像"
api: "POST https://ai.kaiho.cc/v1/tasks"
---

<Note>
本接口采用异步工作模式。成功提交请求后，系统将返回一个任务 ID (`task_id`)。您需要使用该 ID 调用查询接口获取生成结果。生成的图片链接有效期为 72 小时，请尽快下载保存。

[查询任务状态](/cn/api-reference/task/get-status)
</Note>

<RequestExample>

```bash cURL theme={null}
curl https://ai.kaiho.cc/v1/tasks \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "seedream-5.0-lite",
    "prompt": "3D渲染风格的未来太空站",
    "size": "16:9",
    "resolution": "2K",
    "response_format": "png"
  }'
```

```python Python theme={null}
import requests

url = "https://ai.kaiho.cc/v1/tasks"
headers = {
    "Authorization": "Bearer YOUR_API_KEY",
    "Content-Type": "application/json"
}
data = {
    "model": "seedream-5.0-lite",
    "prompt": "3D渲染风格的未来太空站",
    "size": "16:9",
    "resolution": "2K",
    "response_format": "png"
}

response = requests.post(url, headers=headers, json=data)
print(response.json())
```

```javascript JavaScript theme={null}
fetch('https://ai.kaiho.cc/v1/tasks', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'seedream-5.0-lite',
    prompt: '3D渲染风格的未来太空站',
    size: '16:9',
    resolution: '2K',
    response_format: 'png'
  })
})
.then(response => response.json())
.then(data => console.log(data));
```

```go Go theme={null}
package main

import (
	"bytes"
	"encoding/json"
	"fmt"
	"net/http"
)

func main() {
	url := "https://ai.kaiho.cc/v1/tasks"
	payload := map[string]interface{}{
		"model":           "seedream-5.0-lite",
		"prompt":          "3D渲染风格的未来太空站",
		"size":            "16:9",
		"resolution":      "2K",
		"response_format": "png",
	}
	body, _ := json.Marshal(payload)

	req, _ := http.NewRequest("POST", url, bytes.NewBuffer(body))
	req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
	req.Header.Set("Content-Type", "application/json")

	client := &http.Client{}
	resp, _ := client.Do(req)
	defer resp.Body.Close()

	var result map[string]interface{}
	json.NewDecoder(resp.Body).Decode(&result)
	fmt.Println(result)
}
```

</RequestExample>

<ResponseExample>
  ```json 200 theme={null}
  {
    "code": "success",
    "data": "task-wave17486161910001884721"
  }
  ```

  ```json 400 theme={null}
  {
    "code": "invalid_request",
    "message": "请求参数无效",
    "data": null
  }
  ```

  ```json 401 theme={null}
  {
    "code": "authentication_error",
    "message": "身份验证失败，请检查您的API密钥",
    "data": null
  }
  ```

  ```json 402 theme={null}
  {
    "code": "payment_required",
    "message": "账户余额不足，请充值后再试",
    "data": null
  }
  ```

  ```json 403 theme={null}
  {
    "code": "quota_not_enough",
    "message": "用户额度不足",
    "data": null
  }
  ```

  ```json 429 theme={null}
  {
    "code": "rate_limit_error",
    "message": "请求过于频繁，请稍后再试",
    "data": null
  }
  ```

  ```json 500 theme={null}
  {
    "code": "server_error",
    "message": "服务器内部错误，请稍后重试",
    "data": null
  }
  ```
</ResponseExample>

## 认证方式

<ParamField header="Authorization" type="string" required>
  API 密钥，格式为 `Bearer YOUR_API_KEY`。您可以在 [控制台](https://ai.kaiho.cc/dashboard/keys) 获取您的 API Key。
</ParamField>

## 请求参数

<ParamField body="model" type="string" required>
  模型 ID，固定为 `seedream-5.0-lite`。
</ParamField>

<ParamField body="prompt" type="string" required>
  图像生成的文本描述，最大长度 1000 字符。
</ParamField>

<ParamField body="size" type="string" default="1:1">
  生成图像的比例。
  可选值：`1:1`, `2:3`, `3:2`, `3:4`, `4:3`, `4:5`, `5:4`, `9:16`, `16:9`
</ParamField>

<ParamField body="resolution" type="string" default="2K">
  生成分辨率。可选值：`2K`, `3K`。
</ParamField>

<ParamField body="response_format" type="string" default="png">
  返回图片格式。可选值：`png`, `jpeg`。
</ParamField>

<ParamField body="image_urls" type="array">
  参考图像 URL 数组，支持图生图。
</ParamField>

## 响应参数

<ResponseField name="code" type="string">
  响应状态码

  * `success` - 请求成功，任务已提交
</ResponseField>

<ResponseField name="data" type="string">
  任务 ID，格式为 `task-wave{timestamp}{userId}{random}`

  用于通过 [查询任务状态](/cn/api-reference/task/get-status) 接口获取生成结果
</ResponseField>

## 注意事项

- 图片链接有效期长达 **72 小时**，请在此期间完成图片保存。
- 模型支持 3K 超高清分辨率输出。
- 支持图生图以及连续图像生成任务。
