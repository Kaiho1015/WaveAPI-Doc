---
title: "海螺 Hailuo 系列"
description: "MiniMax 海螺视频生成系列模型"
api: "POST https://www.qingbo.dev/v1/tasks"
---

异步处理模式 — 提交请求后返回任务 ID，通过 [查询任务状态](/cn/api-reference/task/status) 接口获取生成结果。视频链接有效期 24 小时。

<RequestExample>
```bash cURL
curl -X POST https://www.qingbo.dev/v1/tasks \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "minimax-hailuo-2.3",
    "action": "generate",
    "prompt": "一只白猫慵懒地躺在阳光下的窗台上",
    "duration": 6,
    "resolution": "1080p"
  }'
```

```python Python
import requests

response = requests.post(
    "https://www.qingbo.dev/v1/tasks",
    headers={"Authorization": "Bearer YOUR_API_KEY"},
    json={
        "model": "minimax-hailuo-2.3",
        "action": "generate",
        "prompt": "一只白猫慵懒地躺在阳光下的窗台上",
        "duration": 6,
        "resolution": "1080p"
    }
)

task_id = response.json()["task_id"]
print(f"任务已提交: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://www.qingbo.dev/v1/tasks', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'minimax-hailuo-2.3',
    action: 'generate',
    prompt: '一只白猫慵懒地躺在阳光下的窗台上',
    duration: 6,
    resolution: '1080p'
  })
});

const { task_id } = await response.json();
console.log(`任务已提交: ${task_id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":      "minimax-hailuo-2.3",
        "action":     "generate",
        "prompt":     "一只白猫慵懒地躺在阳光下的窗台上",
        "duration":   6,
        "resolution": "1080p",
    }

    body, _ := json.Marshal(payload)
    req, _ := http.NewRequest("POST", "https://www.qingbo.dev/v1/tasks", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    result, _ := io.ReadAll(resp.Body)
    fmt.Println(string(result))
}
```

```java Java
import java.net.http.*;
import java.net.URI;

public class Main {
    public static void main(String[] args) throws Exception {
        String payload = """
        {
          "model": "minimax-hailuo-2.3",
          "action": "generate",
          "prompt": "一只白猫慵懒地躺在阳光下的窗台上",
          "duration": 6,
          "resolution": "1080p"
        }
        """;

        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://www.qingbo.dev/v1/tasks"))
            .header("Authorization", "Bearer YOUR_API_KEY")
            .header("Content-Type", "application/json")
            .POST(HttpRequest.BodyPublishers.ofString(payload))
            .build();

        HttpResponse<String> response = client.send(request,
            HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```
</RequestExample>

<ResponseExample>
```json 200
{
  "task_id": "task-wave1775285160b950328499",
  "model": "minimax-hailuo-2.3",
  "action": "generate",
  "status": "queued",
  "created_at": 1775285160040,
  "progress": 0
}
```
</ResponseExample>

## 可用模型

| 模型 ID | 默认分辨率 | 支持时长 | 说明 |
|---------|-----------|---------|------|
| `minimax-hailuo-2.3` | 768P | 6s / 10s | 海螺 2.3，支持角色参考 |
| `minimax-hailuo-2.3-fast` | 768P | 6s / 10s | 海螺 2.3 快速版，仅支持图生视频 |

## 统一参数

<ParamField body="model" type="string" required>
  模型 ID
</ParamField>

<ParamField body="action" type="string" default="generate">
  操作类型
</ParamField>

<ParamField body="prompt" type="string" required>
  视频描述文本
</ParamField>

<ParamField body="duration" type="integer" default="6">
  视频时长（秒）：`6`、`10`
</ParamField>

<ParamField body="resolution" type="string" default="768p">
  输出分辨率，可选值取决于模型，见各模型 Tab
</ParamField>

<ParamField body="image_urls" type="string[]">
  首帧图片 URL，用于图生视频
</ParamField>

<ParamField body="callback_url" type="string">
  Webhook 回调地址
</ParamField>

<ParamField body="extra" type="object">
  透传给海螺的额外参数
</ParamField>

## 模型特定参数

**resolution 可选值：** `768p`、`1080p`

<ParamField body="extra.prompt_optimizer" type="boolean" default="true">
  是否启用提示词优化
</ParamField>

<ParamField body="extra.fast_pretreatment" type="boolean">
  快速预处理模式
</ParamField>

<ParamField body="extra.subject_reference" type="array">
  角色参考，保持角色一致性（仅 minimax-hailuo-2.3）

  ```json
  "extra": {
    "subject_reference": [{
      "type": "character",
      "image": ["https://example.com/character.jpg"]
    }]
  }
  ```
</ParamField>

## 生成模式

| 模式 | 参数 | 支持模型 |
|------|------|---------|
| 文生视频 | `prompt` | minimax-hailuo-2.3 |
| 图生视频 | `prompt` + `image_urls`(1张) | 全部 |
| 角色参考 | `prompt` + `extra.subject_reference` | minimax-hailuo-2.3 |

<Note>
`minimax-hailuo-2.3-fast` 仅支持图生视频模式。
</Note>

## 响应

<ResponseField name="task_id" type="string">
  任务 ID
</ResponseField>

<ResponseField name="model" type="string">
  使用的模型 ID
</ResponseField>

<ResponseField name="action" type="string">
  操作类型
</ResponseField>

<ResponseField name="status" type="string">
  任务状态：`queued`
</ResponseField>

<ResponseField name="created_at" type="integer">
  创建时间（Unix 毫秒时间戳）
</ResponseField>

<ResponseField name="progress" type="integer">
  进度，初始为 `0`
</ResponseField>
