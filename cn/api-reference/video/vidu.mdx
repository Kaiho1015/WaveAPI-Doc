---
title: "Vidu 系列"
description: "生数科技 Vidu 视频生成系列模型"
api: "POST https://www.qingbo.dev/v1/tasks"
---

异步处理模式 — 提交请求后返回任务 ID，通过 [查询任务状态](/cn/api-reference/task/status) 接口获取生成结果。视频链接有效期 24 小时。

<RequestExample>
```bash cURL
curl -X POST https://www.qingbo.dev/v1/tasks \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "viduq3",
    "action": "generate",
    "prompt": "城市夜景延时摄影，霓虹灯光流转",
    "aspect_ratio": "16:9",
    "duration": 5,
    "resolution": "1080p"
  }'
```

```python Python
import requests

response = requests.post(
    "https://www.qingbo.dev/v1/tasks",
    headers={"Authorization": "Bearer YOUR_API_KEY"},
    json={
        "model": "viduq3",
        "action": "generate",
        "prompt": "城市夜景延时摄影，霓虹灯光流转",
        "aspect_ratio": "16:9",
        "duration": 5,
        "resolution": "1080p"
    }
)

task_id = response.json()["task_id"]
print(f"任务已提交: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://www.qingbo.dev/v1/tasks', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'viduq3',
    action: 'generate',
    prompt: '城市夜景延时摄影，霓虹灯光流转',
    aspect_ratio: '16:9',
    duration: 5,
    resolution: '1080p'
  })
});

const { task_id } = await response.json();
console.log(`任务已提交: ${task_id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":        "viduq3",
        "action":       "generate",
        "prompt":       "城市夜景延时摄影，霓虹灯光流转",
        "aspect_ratio": "16:9",
        "duration":     5,
        "resolution":   "1080p",
    }

    body, _ := json.Marshal(payload)
    req, _ := http.NewRequest("POST", "https://www.qingbo.dev/v1/tasks", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    result, _ := io.ReadAll(resp.Body)
    fmt.Println(string(result))
}
```

```java Java
import java.net.http.*;
import java.net.URI;

public class Main {
    public static void main(String[] args) throws Exception {
        String payload = """
        {
          "model": "viduq3",
          "action": "generate",
          "prompt": "城市夜景延时摄影，霓虹灯光流转",
          "aspect_ratio": "16:9",
          "duration": 5,
          "resolution": "1080p"
        }
        """;

        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://www.qingbo.dev/v1/tasks"))
            .header("Authorization", "Bearer YOUR_API_KEY")
            .header("Content-Type", "application/json")
            .POST(HttpRequest.BodyPublishers.ofString(payload))
            .build();

        HttpResponse<String> response = client.send(request,
            HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```
</RequestExample>

<ResponseExample>
```json 200
{
  "task_id": "task-wave1775285160b950328499",
  "model": "viduq3",
  "action": "generate",
  "status": "queued",
  "created_at": 1775285160040,
  "progress": 0
}
```
</ResponseExample>

## 可用模型

| 模型 ID | 说明 |
|---------|------|
| `viduq3` | Vidu Q3，原生音频、16 秒、1080p |
| `viduq3-pro` | Vidu Q3 Pro，最高画质 |
| `viduq3-turbo` | Vidu Q3 Turbo，速度优先 |
| `viduq3-mix` | Vidu Q3 Mix，多参考图多实体一致性 |
| `viduq2` | Vidu Q2 |
| `viduq1` | Vidu Q1 |

## 统一参数

<ParamField body="model" type="string" required>
  模型 ID
</ParamField>

<ParamField body="action" type="string" default="generate">
  操作类型
</ParamField>

<ParamField body="prompt" type="string" required>
  视频描述文本
</ParamField>

<ParamField body="aspect_ratio" type="string" default="16:9">
  宽高比：`16:9`、`9:16`、`1:1`
</ParamField>

<ParamField body="duration" type="integer" default="5">
  视频时长（秒）
</ParamField>

<ParamField body="resolution" type="string" default="1080p">
  分辨率：`720p`、`1080p`
</ParamField>

<ParamField body="seed" type="integer">
  随机种子，`0` 为随机
</ParamField>

<ParamField body="watermark" type="boolean">
  是否添加水印。支持自定义水印图片和位置（通过 extra）
</ParamField>

<ParamField body="image_urls" type="string[]">
  参考图片 URL 数组，数量决定生成模式：
  - 1 张 → 图生视频
  - 2 张 → 首尾帧模式
  - 3+ 张 → 参考图引导（自动使用 viduq2）
</ParamField>

<ParamField body="callback_url" type="string">
  Webhook 回调地址
</ParamField>

<ParamField body="extra" type="object">
  透传参数
</ParamField>

## 模型特定参数

<ParamField body="extra.movement_amplitude" type="string" default="auto">
  运动幅度：`auto`、`small`、`medium`、`large`
</ParamField>

<ParamField body="extra.bgm" type="boolean" default="false">
  是否添加背景音乐
</ParamField>

<ParamField body="extra.wm_url" type="string">
  自定义水印图片 URL（需 `watermark: true`）
</ParamField>

<ParamField body="extra.wm_position" type="integer">
  水印位置：`1`（左上）、`2`（右上）、`3`（左下）、`4`（右下）
</ParamField>

## 生成模式

| 模式 | 参数 | 说明 |
|------|------|------|
| 文生视频 | `prompt` | 纯文本生成 |
| 图生视频 | `prompt` + `image_urls`(1张) | 单图参考 |
| 首尾帧 | `image_urls`(2张) | 起止画面过渡 |
| 参考图引导 | `image_urls`(3+张) | 多图参考（自动切换为 viduq2） |

## 响应

<ResponseField name="task_id" type="string">
  任务 ID
</ResponseField>

<ResponseField name="model" type="string">
  使用的模型 ID
</ResponseField>

<ResponseField name="action" type="string">
  操作类型
</ResponseField>

<ResponseField name="status" type="string">
  任务状态：`queued`
</ResponseField>

<ResponseField name="created_at" type="integer">
  创建时间（Unix 毫秒时间戳）
</ResponseField>

<ResponseField name="progress" type="integer">
  进度，初始为 `0`
</ResponseField>
