---
title: "Video Generation Overview"
description: "Generate videos through the unified /v1/tasks endpoint with support for multiple models"
---

## Overview

WaveAPI supports multiple video generation models, all accessible through the unified async task endpoint `/v1/tasks`. Video generation typically takes longer, so use polling or webhook callbacks to retrieve results.

## Request Endpoint

```
POST https://www.qingbo.dev/v1/tasks
```

## Common Request Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `model` | string | Yes | Model name |
| `action` | string | No | Defaults to `"generate"` |
| `prompt` | string | Yes | Video description text |
| `duration` | integer | No | Video duration (seconds) |
| `aspect_ratio` | string | No | Aspect ratio, e.g., `"16:9"` |
| `resolution` | string | No | Resolution, e.g., `"1080p"` |
| `first_frame` | string | No | First frame image URL |
| `last_frame` | string | No | Last frame image URL |
| `image_urls` | array | No | Reference image URL array |
| `callback_url` | string | No | Webhook callback URL upon completion |
| `extra` | object | No | Model-specific pass-through parameters |

## Response Format

```json
{
  "task_id": "task-wave1775285160b950328499",
  "model": "veo-3.1-fast",
  "action": "generate",
  "status": "queued",
  "created_at": 1775285160040,
  "progress": 0
}
```

## Supported Models

<CardGroup cols={2}>
  <Card title="VEO Series" href="/en/api-reference/video/veo">
    Google VEO 3.1 Fast / Lite / Quality
  </Card>
  <Card title="Seedance Series" href="/en/api-reference/video/seedance">
    ByteDance Seedance 2.0 / 1.5
  </Card>
  <Card title="Sora Series" href="/en/api-reference/video/sora">
    OpenAI Sora 2 / 2 Pro
  </Card>
  <Card title="Kling" href="/en/api-reference/video/kling">
    Kuaishou Kling series
  </Card>
  <Card title="Hailuo" href="/en/api-reference/video/hailuo">
    MiniMax Hailuo 2.3
  </Card>
  <Card title="Vidu Series" href="/en/api-reference/video/vidu">
    Vidu Q3 / Q3 Pro / Q3 Turbo
  </Card>
</CardGroup>

## Generation Modes

| Mode | Parameters | Description |
|------|-----------|-------------|
| Text-to-video | `prompt` | Generate video from text description |
| Image-to-video | `prompt` + `first_frame` or `image_urls` | Generate video from an image |
| First/Last frame | `first_frame` + `last_frame` | Generate a transition video between specified start and end frames |

<Note>
Not all models support all modes. Refer to each model's documentation for specific support details.
</Note>
