---
title: "VEO シリーズ"
description: "Google VEO 動画生成シリーズモデル"
api: "POST https://www.qingbo.dev/v1/tasks"
---

非同期処理モード — リクエスト送信後にタスク ID が返されます。[タスクステータス照会](/jp/api-reference/task/status) インターフェースで生成結果を取得してください。動画リンクの有効期限は24時間です。速やかにダウンロードしてください。

<RequestExample>
```bash cURL
curl -X POST https://www.qingbo.dev/v1/tasks \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "veo-3.1-fast",
    "action": "generate",
    "prompt": "A golden retriever running on a beach at sunset, cinematic lighting, slow motion",
    "aspect_ratio": "16:9",
    "duration": 8,
    "resolution": "1080p"
  }'
```

```python Python
import requests

response = requests.post(
    "https://www.qingbo.dev/v1/tasks",
    headers={"Authorization": "Bearer YOUR_API_KEY"},
    json={
        "model": "veo-3.1-fast",
        "action": "generate",
        "prompt": "A golden retriever running on a beach at sunset, cinematic lighting, slow motion",
        "aspect_ratio": "16:9",
        "duration": 8,
        "resolution": "1080p"
    }
)

task_id = response.json()["task_id"]
print(f"タスクを送信しました: {task_id}")
```

```javascript JavaScript
const response = await fetch('https://www.qingbo.dev/v1/tasks', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer YOUR_API_KEY',
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    model: 'veo-3.1-fast',
    action: 'generate',
    prompt: 'A golden retriever running on a beach at sunset, cinematic lighting, slow motion',
    aspect_ratio: '16:9',
    duration: 8,
    resolution: '1080p'
  })
});

const { task_id } = await response.json();
console.log(`タスクを送信しました: ${task_id}`);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":      "veo-3.1-fast",
        "action":     "generate",
        "prompt":     "A golden retriever running on a beach at sunset, cinematic lighting, slow motion",
        "aspect_ratio": "16:9",
        "duration":   8,
        "resolution": "1080p",
    }

    body, _ := json.Marshal(payload)
    req, _ := http.NewRequest("POST", "https://www.qingbo.dev/v1/tasks", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    result, _ := io.ReadAll(resp.Body)
    fmt.Println(string(result))
}
```

```java Java
import java.net.http.*;
import java.net.URI;

public class Main {
    public static void main(String[] args) throws Exception {
        String payload = """
        {
          "model": "veo-3.1-fast",
          "action": "generate",
          "prompt": "A golden retriever running on a beach at sunset, cinematic lighting, slow motion",
          "aspect_ratio": "16:9",
          "duration": 8,
          "resolution": "1080p"
        }
        """;

        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://www.qingbo.dev/v1/tasks"))
            .header("Authorization", "Bearer YOUR_API_KEY")
            .header("Content-Type", "application/json")
            .POST(HttpRequest.BodyPublishers.ofString(payload))
            .build();

        HttpResponse<String> response = client.send(request,
            HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```
</RequestExample>

<ResponseExample>
```json 200
{
  "task_id": "task-wave1775285160b950328499",
  "model": "veo-3.1-fast",
  "action": "generate",
  "status": "queued",
  "created_at": 1775285160040,
  "progress": 0
}
```
</ResponseExample>

## 利用可能なモデル

| モデル ID | 説明 |
|---------|------|
| `veo-3.1-fast` | VEO 3.1 高速版 |
| `veo-3.1-lite` | VEO 3.1 軽量版、低コスト |
| `veo-3.1-quality` | VEO 3.1 高画質版 |

## 統一パラメータ

<ParamField body="model" type="string" required>
  モデル ID
</ParamField>

<ParamField body="action" type="string" default="generate">
  操作タイプ
</ParamField>

<ParamField body="prompt" type="string" required>
  動画の説明テキスト。VEO 3+ ではプロンプト内で効果音やセリフを記述でき、モデルが自動的に対応する音声を生成します
</ParamField>

<ParamField body="aspect_ratio" type="string" default="16:9">
  アスペクト比：`16:9`、`9:16`
</ParamField>

<ParamField body="duration" type="integer" default="8">
  動画の長さ（秒）：`4`、`6`、`8`
</ParamField>

<ParamField body="resolution" type="string" default="720p">
  出力解像度、選択可能な値はモデルにより異なります。各モデルの Tab を参照
</ParamField>

<ParamField body="negative_prompt" type="string">
  表示させたくない内容を直接記述してください（否定文は使用しないでください）
</ParamField>

<ParamField body="seed" type="integer">
  ランダムシード、再現性を向上（完全一致は保証されません）。VEO 3.1 シリーズのみ対応
</ParamField>

<ParamField body="callback_url" type="string">
  Webhook コールバックアドレス
</ParamField>

<ParamField body="extra" type="object">
  Google VEO にパススルーされる追加パラメータ、各モデルの Tab を参照
</ParamField>

## モデル固有のパラメータ

<Tabs>
  <Tab title="VEO 3.1 Fast">
    **resolution の選択可能な値：** `720p`、`1080p`、`4k`

    <ParamField body="extra.personGeneration" type="string" default="allow_all">
      人物生成の制御：

      - `allow_all` — 全ての人物を許可
      - `allow_adult` — 成人のみ許可
      - `dont_allow` — 人物生成を禁止
    </ParamField>
  </Tab>

  <Tab title="VEO 3.1 Lite">
    **resolution の選択可能な値：** `720p`、`1080p`（4K 非対応）

    低コストの軽量版。

    <ParamField body="extra.personGeneration" type="string" default="allow_all">
      人物生成の制御：`allow_all`、`allow_adult`、`dont_allow`
    </ParamField>
  </Tab>

  <Tab title="VEO 3.1 Quality">
    **resolution の選択可能な値：** `720p`、`1080p`、`4k`

    最高画質版。

    <ParamField body="extra.personGeneration" type="string" default="allow_all">
      人物生成の制御：`allow_all`、`allow_adult`、`dont_allow`
    </ParamField>
  </Tab>
</Tabs>

## 制約条件

| 条件 | 要件 |
|------|------|
| 解像度 `1080p` または `4k` | `duration` は `8` である必要があります |
| 解像度 `4k` | VEO 3.1 Fast および Quality のみ対応、Lite は非対応 |
| 出力フレームレート | 24 FPS |
| 出力フォーマット | MP4 |

<Note>
VEO 3+ はネイティブで音声生成に対応しており、追加パラメータは不要です。プロンプト内で効果音を記述するだけです（例："the sound of waves crashing"）。
</Note>

## レスポンス

<ResponseField name="task_id" type="string">
  タスク ID
</ResponseField>

<ResponseField name="model" type="string">
  使用されたモデル ID
</ResponseField>

<ResponseField name="action" type="string">
  操作タイプ
</ResponseField>

<ResponseField name="status" type="string">
  タスクステータス：`queued`
</ResponseField>

<ResponseField name="created_at" type="integer">
  作成時間（Unix ミリ秒タイムスタンプ）
</ResponseField>

<ResponseField name="progress" type="integer">
  進捗、初期値は `0`
</ResponseField>
