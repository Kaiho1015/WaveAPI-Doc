---
title: "Gemini 画像シリーズ"
description: "Google Gemini 画像生成シリーズモデル"
api: "POST https://qingbo.dev/v1/tasks"
---

非同期処理モード — リクエスト送信後にタスク ID が返されます。[タスクステータス照会](/jp/api-reference/task/status) インターフェースで生成結果を取得してください。

<RequestExample>
```bash cURL
curl -X POST https://qingbo.dev/v1/tasks \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gemini-3.1-flash-image-preview",
    "prompt": "一只橘猫趴在窗台上晒太阳，水彩画风格",
    "size": "16:9"
  }'
```

```python Python
from openai import OpenAI

client = OpenAI(
    base_url="https://qingbo.dev/v1",
    api_key="YOUR_API_KEY"
)

response = client.images.generate(
    model="gemini-3.1-flash-image-preview",
    prompt="一只橘猫趴在窗台上晒太阳，水彩画风格",
    size="16:9"
)

print(response.data[0])
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://qingbo.dev/v1',
  apiKey: 'YOUR_API_KEY'
});

const response = await client.images.generate({
  model: 'gemini-3.1-flash-image-preview',
  prompt: '一只橘猫趴在窗台上晒太阳，水彩画风格',
  size: '16:9'
});

console.log(response.data[0]);
```

```go Go
package main

import (
    "bytes"
    "encoding/json"
    "fmt"
    "io"
    "net/http"
)

func main() {
    payload := map[string]interface{}{
        "model":  "gemini-3.1-flash-image-preview",
        "prompt": "一只橘猫趴在窗台上晒太阳，水彩画风格",
        "size":   "16:9",
    }

    body, _ := json.Marshal(payload)
    req, _ := http.NewRequest("POST", "https://qingbo.dev/v1/tasks", bytes.NewBuffer(body))
    req.Header.Set("Authorization", "Bearer YOUR_API_KEY")
    req.Header.Set("Content-Type", "application/json")

    resp, _ := http.DefaultClient.Do(req)
    defer resp.Body.Close()
    result, _ := io.ReadAll(resp.Body)
    fmt.Println(string(result))
}
```

```java Java
import java.net.http.*;
import java.net.URI;

public class Main {
    public static void main(String[] args) throws Exception {
        String payload = """
        {
          "model": "gemini-3.1-flash-image-preview",
          "prompt": "一只橘猫趴在窗台上晒太阳，水彩画风格",
          "size": "16:9"
        }
        """;

        HttpClient client = HttpClient.newHttpClient();
        HttpRequest request = HttpRequest.newBuilder()
            .uri(URI.create("https://qingbo.dev/v1/tasks"))
            .header("Authorization", "Bearer YOUR_API_KEY")
            .header("Content-Type", "application/json")
            .POST(HttpRequest.BodyPublishers.ofString(payload))
            .build();

        HttpResponse<String> response = client.send(request,
            HttpResponse.BodyHandlers.ofString());
        System.out.println(response.body());
    }
}
```
</RequestExample>

<ResponseExample>
```json 200
{
  "code": "success",
  "data": "task-wave17200000001234567890abcdef"
}
```
</ResponseExample>

## 利用可能なモデル

| モデル ID | 説明 |
|---------|------|
| `gemini-2.5-flash-image-preview` | Gemini 2.5 Flash 画像プレビュー版 |
| `gemini-3-pro-image-preview` | Gemini 3 Pro 画像プレビュー版 |
| `gemini-3.1-flash-image-preview` | Gemini 3.1 Flash 画像プレビュー版、高解像度と検索強化に対応 |

## 共通パラメータ

<ParamField body="model" type="string" required>
  モデル ID、上記のモデルリストを参照
</ParamField>

<ParamField body="prompt" type="string" required>
  画像の説明テキスト、最大1000文字
</ParamField>

<ParamField body="image_urls" type="array">
  参照画像 URL 配列、最大14枚、1枚あたり10MB以下
</ParamField>

<ParamField body="n" type="integer" default="1">
  生成数、固定値 1
</ParamField>

## 各モデルの差異パラメータ

<Tabs>
  <Tab title="Gemini 2.5 Flash">
    <ParamField body="size" type="string" default="1:1">
      画像サイズ比率

      対応：`1:1`、`2:3`、`3:2`、`3:4`、`4:3`、`4:5`、`5:4`、`9:16`、`16:9`、`21:9`
    </ParamField>

    <ParamField body="resolution" type="string" default="1K">
      出力解像度：`1K`
    </ParamField>
  </Tab>

  <Tab title="Gemini 3 Pro">
    <ParamField body="size" type="string" default="1:1">
      画像サイズ比率

      対応：`1:1`、`2:3`、`3:2`、`3:4`、`4:3`、`4:5`、`5:4`、`9:16`、`16:9`、`21:9`
    </ParamField>

    <ParamField body="resolution" type="string" default="1K">
      出力解像度：`1K`
    </ParamField>
  </Tab>

  <Tab title="Gemini 3.1 Flash">
    <ParamField body="size" type="string" default="1:1">
      画像サイズ比率

      対応：`1:1`、`2:3`、`3:2`、`3:4`、`4:3`、`4:5`、`5:4`、`9:16`、`16:9`、`21:9`、`1:4`、`4:1`、`1:8`、`8:1`
    </ParamField>

    <ParamField body="resolution" type="string" default="1K">
      出力解像度：`1K`、`2K`、`4K`
    </ParamField>

    <ParamField body="google_search_enhancement" type="boolean" default="false">
      Google 検索強化を有効にし、画像のリアリティを向上
    </ParamField>
  </Tab>
</Tabs>

## レスポンス

タスク ID を返します。[タスクステータス照会](/jp/api-reference/task/status) で最終的な画像結果を取得してください。

<ResponseField name="code" type="string">
  ステータスコード、成功時は `"success"`
</ResponseField>

<ResponseField name="data" type="string">
  タスク ID、形式は `task-wave{timestamp}{userId}{random}`
</ResponseField>
