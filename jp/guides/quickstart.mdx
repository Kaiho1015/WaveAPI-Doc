---
title: "クイックスタート"
description: "5分で WaveAPI を始めましょう"
---

## 前提条件

- WaveAPI アカウント（[登録](https://ai.kaiho.cc/login)）
- API キー
- 基本的なプログラミング知識

## API キーの取得

<Steps>
  <Step title="コンソールにログイン">
    [Dashboard](https://ai.kaiho.cc/dashboard/keys) にアクセスしてログインしてください
  </Step>
  <Step title="キーの作成">
    キー管理ページで「新しいキーを作成」をクリックしてください
  </Step>
  <Step title="キーの保存">
    API キーをコピーして安全に保存してください。キーは一度しか表示されません
  </Step>
</Steps>

<Warning>
フロントエンドのコードや公開リポジトリで API Key を公開しないでください。
</Warning>

## 最初のリクエストを送信

WaveAPI は OpenAI SDK と完全に互換性があり、`base_url` を置き換えるだけで利用できます。

<CodeGroup>

```python Python
from openai import OpenAI

client = OpenAI(
    base_url="https://ai.kaiho.cc/v1",
    api_key="YOUR_API_KEY"
)

response = client.chat.completions.create(
    model="gpt-4o",
    messages=[
        {"role": "user", "content": "你好，介绍一下你自己"}
    ]
)

print(response.choices[0].message.content)
```

```javascript JavaScript
import OpenAI from 'openai';

const client = new OpenAI({
  baseURL: 'https://ai.kaiho.cc/v1',
  apiKey: 'YOUR_API_KEY'
});

const response = await client.chat.completions.create({
  model: 'gpt-4o',
  messages: [
    { role: 'user', content: '你好，介绍一下你自己' }
  ]
});

console.log(response.choices[0].message.content);
```

```bash cURL
curl https://ai.kaiho.cc/v1/chat/completions \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -d '{
    "model": "gpt-4o",
    "messages": [
      {"role": "user", "content": "你好，介绍一下你自己"}
    ]
  }'
```

</CodeGroup>

## レスポンスの理解

```json
{
  "id": "chatcmpl-123",
  "object": "chat.completion",
  "created": 1677652288,
  "model": "gpt-4o",
  "choices": [{
    "index": 0,
    "message": {
      "role": "assistant",
      "content": "你好！我是一个 AI 助手..."
    },
    "finish_reason": "stop"
  }],
  "usage": {
    "prompt_tokens": 9,
    "completion_tokens": 12,
    "total_tokens": 21
  }
}
```

## 次のステップ

<CardGroup cols={2}>
  <Card title="認証とキー" href="/jp/docs/authentication">
    認証方式とキー管理について
  </Card>
  <Card title="同期と非同期" href="/jp/docs/sync-async">
    各エンドポイントの呼び出し方式について
  </Card>
  <Card title="テキスト生成 API" href="/jp/api-reference/text/general-chat">
    チャット API の完全なパラメータとオプション
  </Card>
  <Card title="タスクシステム" href="/jp/docs/task-system">
    画像・動画の非同期生成フロー
  </Card>
</CardGroup>
